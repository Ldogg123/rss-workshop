# Compatibility override for existing installations with an explicit RSS_IMAGE.
# Published images are now the default; new installations do not need this file.
# Apply last to remove an explicit local build and select the chosen tag/digest.
services:
  rss-workshop:
    build: !reset null
    pull_policy: !reset null
    image: ${RSS_IMAGE:?Set RSS_IMAGE to a tagged image or digest}
